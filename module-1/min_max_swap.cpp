#include <iostream>
#include <algorithm>
using namespace std;

int main()
{

    int a, b, c;
    cin >> a >> b >> c;

    // if(a<b) cout<<a<<endl;
    // else cout <<b << endl;

    // advance for C++
    cout << min(a, b) << endl;
    // when more than 2 values
    // cout << min({a, b, c}) << endl;

    // cout << max(a, b) << endl;
    // cout << max({a, b, c}) << endl;


    // int x,y;
    // cin >> x >> y;

    // int temp = x;
    // x = y;
    // y = temp;

    // swap(x,y);

    // cout << x << endl << y;


    return 0;
}